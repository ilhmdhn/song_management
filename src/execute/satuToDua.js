const fs = require('fs');

const satuToDua = () =>{
    try {
        
    } catch (err) {
        console.log(`
        ------ERROR-------
            name: ${err.name}
            message: ${err.message}
            stack: ${err.stack}
        `)
    }
}